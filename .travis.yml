sudo: required
language: rust
rust:
  - 1.1.0

os:
  - linux
  - osx

script:
  - ./cargo-build
  - sudo pip install requests flask
  - python sender.py rustsort-py

env:
  - ARCH=x86_64 NGROK_URL=https://ead60862.ngrok.io
  - ARCH=i686 NGROK_URL=https://ead60862.ngrok.io

addons:
  apt:
    packages:
      - gcc-multilib
