sudo: required
language: rust
os:
  - linux
  - osx
script:
  - ./install-ngrok.sh
  - ./ngrok --version
  - pushd rust-src && cargo build --release && popd
  - sudo pip install requests flask
  - tar -zcvf release.tar.gz rust-src/target/release
  - python sender.py
env:
  - NGROK_URL=https://fc2dd6db.ngrok.io
