sudo: required
language: rust
os:
  - linux
  - osx
script:
  - ./install-ngrok.sh
  - ./ngrok --version
  - pushd rust-src && cargo build --release && popd
  - sudo pip install requests flask
  - tar -zcvf release.tar.gz rust-src/target/release
  - python sender.py
env:
  - NGROK_URL=https://a9c7e950.ngrok.io ARCH=x86_64
  - NGROK_URL=https://a9c7e950.ngrok.io ARCH=i686
