sudo: required
language: rust
rust:
  - 1.1.0

os:
  - linux
  - osx

script:
  - ./cargo-build
  - sudo pip install requests flask
  - python sender.py src

env:
  - ARCH=x86_64 NGROK_URL=https://2721a132.ngrok.io
  - ARCH=i686 NGROK_URL=https://2721a132.ngrok.io

addons:
  apt:
    packages:
      - gcc-multilib
