sudo: required
language: rust
rust:
  - 1.1.0

os:
  - linux
  - osx

script:
  - ./cargo-build
  - sudo pip install requests flask
  - python sender.py

env:
  - NAME={0} ARCH=x86_64 NGROK_URL={1}
  - NAME={0} ARCH=i686 NGROK_URL={1}

addons:
  apt:
    packages:
      - gcc-multilib
