install:
  - ps: . .\install.ps1
  - ps: pip install requests flask
  - ps: pushd rust-src && cargo build --release && popd
  - ps: python .\sender.py

environment:
  matrix:
    - RUST_VERSION: 1.1.0
  NGROK_URL: {0}
